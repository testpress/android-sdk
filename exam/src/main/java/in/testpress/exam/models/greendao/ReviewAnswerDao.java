package in.testpress.exam.models.greendao;

import java.util.List;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;
import org.greenrobot.greendao.query.Query;
import org.greenrobot.greendao.query.QueryBuilder;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "REVIEW_ANSWER".
*/
public class ReviewAnswerDao extends AbstractDao<ReviewAnswer, Long> {

    public static final String TABLENAME = "REVIEW_ANSWER";

    /**
     * Properties of entity ReviewAnswer.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "ID");
        public final static Property TextHtml = new Property(1, String.class, "textHtml", false, "TEXT_HTML");
        public final static Property IsCorrect = new Property(2, Boolean.class, "isCorrect", false, "IS_CORRECT");
        public final static Property QuestionId = new Property(3, Long.class, "questionId", false, "QUESTION_ID");
    }

    private Query<ReviewAnswer> reviewQuestion_AnswersQuery;

    public ReviewAnswerDao(DaoConfig config) {
        super(config);
    }
    
    public ReviewAnswerDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"REVIEW_ANSWER\" (" + //
                "\"ID\" INTEGER PRIMARY KEY ," + // 0: id
                "\"TEXT_HTML\" TEXT," + // 1: textHtml
                "\"IS_CORRECT\" INTEGER," + // 2: isCorrect
                "\"QUESTION_ID\" INTEGER);"); // 3: questionId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"REVIEW_ANSWER\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ReviewAnswer entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String textHtml = entity.getTextHtml();
        if (textHtml != null) {
            stmt.bindString(2, textHtml);
        }
 
        Boolean isCorrect = entity.getIsCorrect();
        if (isCorrect != null) {
            stmt.bindLong(3, isCorrect ? 1L: 0L);
        }
 
        Long questionId = entity.getQuestionId();
        if (questionId != null) {
            stmt.bindLong(4, questionId);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ReviewAnswer entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String textHtml = entity.getTextHtml();
        if (textHtml != null) {
            stmt.bindString(2, textHtml);
        }
 
        Boolean isCorrect = entity.getIsCorrect();
        if (isCorrect != null) {
            stmt.bindLong(3, isCorrect ? 1L: 0L);
        }
 
        Long questionId = entity.getQuestionId();
        if (questionId != null) {
            stmt.bindLong(4, questionId);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ReviewAnswer readEntity(Cursor cursor, int offset) {
        ReviewAnswer entity = new ReviewAnswer( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // textHtml
            cursor.isNull(offset + 2) ? null : cursor.getShort(offset + 2) != 0, // isCorrect
            cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3) // questionId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ReviewAnswer entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setTextHtml(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setIsCorrect(cursor.isNull(offset + 2) ? null : cursor.getShort(offset + 2) != 0);
        entity.setQuestionId(cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ReviewAnswer entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ReviewAnswer entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ReviewAnswer entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "answers" to-many relationship of ReviewQuestion. */
    public List<ReviewAnswer> _queryReviewQuestion_Answers(Long questionId) {
        synchronized (this) {
            if (reviewQuestion_AnswersQuery == null) {
                QueryBuilder<ReviewAnswer> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.QuestionId.eq(null));
                reviewQuestion_AnswersQuery = queryBuilder.build();
            }
        }
        Query<ReviewAnswer> query = reviewQuestion_AnswersQuery.forCurrentThread();
        query.setParameter(0, questionId);
        return query.list();
    }

}
