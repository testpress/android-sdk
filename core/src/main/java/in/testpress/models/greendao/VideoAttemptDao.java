package in.testpress.models.greendao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "VIDEO_ATTEMPT".
*/
public class VideoAttemptDao extends AbstractDao<VideoAttempt, Long> {

    public static final String TABLENAME = "VIDEO_ATTEMPT";

    /**
     * Properties of entity VideoAttempt.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "ID");
        public final static Property LastPosition = new Property(1, String.class, "lastPosition", false, "LAST_POSITION");
        public final static Property State = new Property(2, Integer.class, "state", false, "STATE");
        public final static Property WatchedDuration = new Property(3, String.class, "watchedDuration", false, "WATCHED_DURATION");
        public final static Property VideoContentId = new Property(4, Long.class, "videoContentId", false, "VIDEO_CONTENT_ID");
    }

    private DaoSession daoSession;


    public VideoAttemptDao(DaoConfig config) {
        super(config);
    }
    
    public VideoAttemptDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"VIDEO_ATTEMPT\" (" + //
                "\"ID\" INTEGER PRIMARY KEY ," + // 0: id
                "\"LAST_POSITION\" TEXT," + // 1: lastPosition
                "\"STATE\" INTEGER," + // 2: state
                "\"WATCHED_DURATION\" TEXT," + // 3: watchedDuration
                "\"VIDEO_CONTENT_ID\" INTEGER);"); // 4: videoContentId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"VIDEO_ATTEMPT\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, VideoAttempt entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String lastPosition = entity.getLastPosition();
        if (lastPosition != null) {
            stmt.bindString(2, lastPosition);
        }
 
        Integer state = entity.getState();
        if (state != null) {
            stmt.bindLong(3, state);
        }
 
        String watchedDuration = entity.getWatchedDuration();
        if (watchedDuration != null) {
            stmt.bindString(4, watchedDuration);
        }
 
        Long videoContentId = entity.getVideoContentId();
        if (videoContentId != null) {
            stmt.bindLong(5, videoContentId);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, VideoAttempt entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String lastPosition = entity.getLastPosition();
        if (lastPosition != null) {
            stmt.bindString(2, lastPosition);
        }
 
        Integer state = entity.getState();
        if (state != null) {
            stmt.bindLong(3, state);
        }
 
        String watchedDuration = entity.getWatchedDuration();
        if (watchedDuration != null) {
            stmt.bindString(4, watchedDuration);
        }
 
        Long videoContentId = entity.getVideoContentId();
        if (videoContentId != null) {
            stmt.bindLong(5, videoContentId);
        }
    }

    @Override
    protected final void attachEntity(VideoAttempt entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public VideoAttempt readEntity(Cursor cursor, int offset) {
        VideoAttempt entity = new VideoAttempt( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // lastPosition
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // state
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // watchedDuration
            cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4) // videoContentId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, VideoAttempt entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setLastPosition(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setState(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setWatchedDuration(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setVideoContentId(cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(VideoAttempt entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(VideoAttempt entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(VideoAttempt entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getVideoDao().getAllColumns());
            builder.append(" FROM VIDEO_ATTEMPT T");
            builder.append(" LEFT JOIN VIDEO T0 ON T.\"VIDEO_CONTENT_ID\"=T0.\"ID\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected VideoAttempt loadCurrentDeep(Cursor cursor, boolean lock) {
        VideoAttempt entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Video videoContent = loadCurrentOther(daoSession.getVideoDao(), cursor, offset);
        entity.setVideoContent(videoContent);

        return entity;    
    }

    public VideoAttempt loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<VideoAttempt> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<VideoAttempt> list = new ArrayList<VideoAttempt>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<VideoAttempt> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<VideoAttempt> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
