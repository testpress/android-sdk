package in.testpress.models.greendao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "INSTALLMENT".
*/
public class InstallmentDao extends AbstractDao<Installment, Long> {

    public static final String TABLENAME = "INSTALLMENT";

    /**
     * Properties of entity Installment.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "ID");
        public final static Property Order = new Property(1, Integer.class, "order", false, "ORDER");
        public final static Property Price = new Property(2, String.class, "price", false, "PRICE");
        public final static Property IsPaid = new Property(3, Boolean.class, "isPaid", false, "IS_PAID");
        public final static Property PaidOn = new Property(4, String.class, "paidOn", false, "PAID_ON");
        public final static Property IsCurrentInstallment = new Property(5, Boolean.class, "isCurrentInstallment", false, "IS_CURRENT_INSTALLMENT");
        public final static Property DueDate = new Property(6, String.class, "dueDate", false, "DUE_DATE");
        public final static Property InstallmentPlanId = new Property(7, Long.class, "installmentPlanId", false, "INSTALLMENT_PLAN_ID");
    }

    private DaoSession daoSession;


    public InstallmentDao(DaoConfig config) {
        super(config);
    }
    
    public InstallmentDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"INSTALLMENT\" (" + //
                "\"ID\" INTEGER PRIMARY KEY ," + // 0: id
                "\"ORDER\" INTEGER," + // 1: order
                "\"PRICE\" TEXT," + // 2: price
                "\"IS_PAID\" INTEGER," + // 3: isPaid
                "\"PAID_ON\" TEXT," + // 4: paidOn
                "\"IS_CURRENT_INSTALLMENT\" INTEGER," + // 5: isCurrentInstallment
                "\"DUE_DATE\" TEXT," + // 6: dueDate
                "\"INSTALLMENT_PLAN_ID\" INTEGER);"); // 7: installmentPlanId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"INSTALLMENT\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Installment entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer order = entity.getOrder();
        if (order != null) {
            stmt.bindLong(2, order);
        }
 
        String price = entity.getPrice();
        if (price != null) {
            stmt.bindString(3, price);
        }
 
        Boolean isPaid = entity.getIsPaid();
        if (isPaid != null) {
            stmt.bindLong(4, isPaid ? 1L: 0L);
        }
 
        String paidOn = entity.getPaidOn();
        if (paidOn != null) {
            stmt.bindString(5, paidOn);
        }
 
        Boolean isCurrentInstallment = entity.getIsCurrentInstallment();
        if (isCurrentInstallment != null) {
            stmt.bindLong(6, isCurrentInstallment ? 1L: 0L);
        }
 
        String dueDate = entity.getDueDate();
        if (dueDate != null) {
            stmt.bindString(7, dueDate);
        }
 
        Long installmentPlanId = entity.getInstallmentPlanId();
        if (installmentPlanId != null) {
            stmt.bindLong(8, installmentPlanId);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Installment entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer order = entity.getOrder();
        if (order != null) {
            stmt.bindLong(2, order);
        }
 
        String price = entity.getPrice();
        if (price != null) {
            stmt.bindString(3, price);
        }
 
        Boolean isPaid = entity.getIsPaid();
        if (isPaid != null) {
            stmt.bindLong(4, isPaid ? 1L: 0L);
        }
 
        String paidOn = entity.getPaidOn();
        if (paidOn != null) {
            stmt.bindString(5, paidOn);
        }
 
        Boolean isCurrentInstallment = entity.getIsCurrentInstallment();
        if (isCurrentInstallment != null) {
            stmt.bindLong(6, isCurrentInstallment ? 1L: 0L);
        }
 
        String dueDate = entity.getDueDate();
        if (dueDate != null) {
            stmt.bindString(7, dueDate);
        }
 
        Long installmentPlanId = entity.getInstallmentPlanId();
        if (installmentPlanId != null) {
            stmt.bindLong(8, installmentPlanId);
        }
    }

    @Override
    protected final void attachEntity(Installment entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Installment readEntity(Cursor cursor, int offset) {
        Installment entity = new Installment( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // order
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // price
            cursor.isNull(offset + 3) ? null : cursor.getShort(offset + 3) != 0, // isPaid
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // paidOn
            cursor.isNull(offset + 5) ? null : cursor.getShort(offset + 5) != 0, // isCurrentInstallment
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // dueDate
            cursor.isNull(offset + 7) ? null : cursor.getLong(offset + 7) // installmentPlanId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Installment entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setOrder(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setPrice(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setIsPaid(cursor.isNull(offset + 3) ? null : cursor.getShort(offset + 3) != 0);
        entity.setPaidOn(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setIsCurrentInstallment(cursor.isNull(offset + 5) ? null : cursor.getShort(offset + 5) != 0);
        entity.setDueDate(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setInstallmentPlanId(cursor.isNull(offset + 7) ? null : cursor.getLong(offset + 7));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Installment entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Installment entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Installment entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getInstallmentPlanDao().getAllColumns());
            builder.append(" FROM INSTALLMENT T");
            builder.append(" LEFT JOIN INSTALLMENT_PLAN T0 ON T.\"INSTALLMENT_PLAN_ID\"=T0.\"ID\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Installment loadCurrentDeep(Cursor cursor, boolean lock) {
        Installment entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        InstallmentPlan installmentPlan = loadCurrentOther(daoSession.getInstallmentPlanDao(), cursor, offset);
        entity.setInstallmentPlan(installmentPlan);

        return entity;    
    }

    public Installment loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Installment> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Installment> list = new ArrayList<Installment>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Installment> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Installment> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
